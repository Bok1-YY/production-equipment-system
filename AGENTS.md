# 仓库开发硬规则

本文件适用于整个 `production-equipment-system` 仓库。任何人工开发或自动化代理都必须遵守；不得因为赶进度、测试方便或“可以重新生成”而跳过。

## 1. 数据库与现场数据不可破坏

- `equipment-system/factory-data/equipment.db`（Windows 日常运行库）、对应的 WAL/SHM、`equipment-system/data/equipment.db`、所有备份数据库、用户提供的数据库、附件目录都视为不可替代的现场数据。
- 禁止删除、清空、覆盖、重建、替换、回滚或拿测试数据污染上述文件。不得把演示数据库、空数据库或测试数据库覆盖到正式数据库路径。
- 拉取代码、切换分支、清理构建产物和修复启动器时，必须保留所有数据库、WAL/SHM、备份和附件。不得用 `git clean`、递归删除或宽泛通配符触碰数据目录。
- 自动化测试、浏览器冒烟、负载测试、Android 联调和迁移验证必须使用独立的临时数据库；必须显式设置唯一的 `YSM_DB_PATH`，且路径不得位于 `equipment-system/data`、`equipment-system/factory-data` 或 `equipment-system/demo-data`。
- 任何可能写入正式数据库的迁移、恢复、导入或路径切换，必须先停止服务，使用 SQLite 在线备份/VACUUM INTO 或仓库备份脚本生成带时间戳的一致性备份，并校验备份可读。没有用户明确授权，不得对正式库执行此类操作。
- 数据库异常时先做只读检查，分别核对主文件、WAL/SHM、实际进程环境、表数量和核心业务数据量；不能仅凭主文件大小判断数据库为空或损坏。

## 2. 代码改动必须经过 Android 真机测试

- 修改服务端、网页、移动端、扫码/相机/权限/通知、启动器、数据库迁移或部署代码后，除了相关自动化测试，还必须在真实 Android 手机上完成安装和运行验证；浏览器、模拟器、Gradle 构建成功都不能替代真机。
- 真机验收至少包括：APK 构建与验签、ADB 覆盖安装、冷启动、登录、首页数据加载，以及本次改动影响的核心流程。涉及相机、扫码、权限、通知或附件时，必须在真机完成对应硬件链路。
- 真机测试必须连接独立测试数据库/测试服务，禁止用自动化脚本操作正式数据库。
- 没有可用真机或真机验证失败时，不得宣称开发完成；必须明确报告“等待真机验证”及尚未覆盖的步骤。

## 3. 重大改动必须同步文档

- 重大改动包括但不限于：业务流程、数据库结构或路径、API、权限、安全规则、启动/部署/备份方式、Android 行为、扫码/相机/通知、生产配置和用户操作入口变化。
- 每次重大改动必须在同一批修改中同步：
  - `README.md`
  - `equipment-system/README.md`
  - `equipment-system/DEVGUIDE.md`
  - `equipment-system/开发日志.md`
- README 说明用户如何使用以及必要风险；DEVGUIDE 记录实现、约束、数据路径和验证方法；开发日志记录日期、问题、改动和实际验证结果。四处内容必须与代码一致，不得提前写“已通过”或省略未完成的真机测试。
