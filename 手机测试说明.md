# 优胜美设备管理安卓手机测试版

这个目录可作为原系统的独立测试副本。手机产生的报修、照片和其他修改只会写入
当前项目的 `equipment-system/data/`，不会写入其他部署实例。

## 第一次打包

1. 电脑和安卓手机连接同一个 Wi-Fi。
2. 双击“打包并联调安卓测试版”。
3. 首次运行确认 Android SDK 许可并等待便携式 JDK 21 与构建工具下载完成；以后打包会直接复用缓存。
4. 脚本会自动完成后端测试、浏览器测试、Android 单元测试与 lint、APK 地址和下载链路检查；任一步失败都会停止，不会把有问题的包当成成功包。
5. 完成后先看 `安装包/最近一次联调报告.txt`，再打开 `安装包/手机下载安装二维码.png`，用手机扫码下载安装。
6. 如果电脑通过 USB 连接了已允许调试的手机，脚本还会自动运行设备端测试、覆盖安装并启动 App。
7. 安卓如提示“禁止安装未知应用”，只对当前浏览器临时允许即可。

APK 会把打包时电脑的局域网 IP 作为默认地址。电脑换了 Wi-Fi 或 IP 发生变化时，
可以在 App 顶部点“服务器”直接修改；如果旧地址已经完全打不开，离线页也有同一套配置。

## 日常测试

- 双击“启动手机测试服务”，电脑必须保持开机。
- 手机打开“优胜美设备管理（测试）”。
- 登录后点右下角“扫码”，把设备铭牌二维码放入取景框。普工直接进入报修，技术员和管理员可选择巡检或报修。
- 首次扫码时请允许相机权限；如果误点拒绝，可到安卓“设置 → 应用 → 优胜美设备管理（测试）→ 权限”中重新允许相机。
- 二级技术员登录后请允许通知权限。系统正常时每 30 秒通过厂区 Wi-Fi 检查待接单报修，失败时逐步退避、最长 5 分钟再试；通知会显示产线、设备和故障，点击直接打开对应工单。
- 技术员后台监听期间会有一条低优先级的“报修通知已开启”常驻通知。待接单数量会同步到支持角标的安卓桌面；接单后相应通知和角标会自动消失。
- 测试结束双击“停止手机测试服务”。
- 测试服务端口固定为 `8788`，原系统仍使用 `8787`。
- 局域网测试地址填写完整根地址，例如 `http://192.168.31.185:8788`，先点“测试连接”，成功后再“保存并重新连接”。
- 正式上云后填写 HTTPS 域名，例如 `https://equipment.example.com`；这里配置的是后端入口，不是 `8.8.8.8` 这类 DNS 解析器。
- “恢复安装包默认地址”会清除手机上的自定义值。测试包只允许私网 IP 使用 HTTP，正式包强制使用 HTTPS。
- 如果电脑换 Wi‑Fi 或开启了新的 VPN/TUN，也可以重新执行“打包并联调安卓测试版”；脚本会拒绝把虚拟网卡地址写进 APK。

## 重要限制

- 这是局域网 HTTP 测试包，不是生产发布包，不要放到公网。
- 只在可信 Wi-Fi 下测试，避免使用正式敏感密码。
- 扫码需要安装本次生成的新 APK，旧 APK 没有相机扫码组件。最低支持 Android 8。
- 公网推送和离线同步尚未加入；扫码识别及报修通知仍需通过厂区 Wi-Fi 连接电脑上的测试服务。
- 当前报修通知是厂区局域网监听方案，不依赖公网推送。手机离开厂区 Wi-Fi、电脑服务关闭或用户在系统里“强制停止”App 时无法收取；恢复网络或重新打开 App 后会自动继续。
- 手机拍照使用现有“拍照/选图”按钮。
